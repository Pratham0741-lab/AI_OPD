"""
Shared state container (GPU-free).

Previously held GPU model references. Now only used as a placeholder
for any future shared state needs across modules.
"""

# No GPU models needed — Twilio handles all speech recognition
